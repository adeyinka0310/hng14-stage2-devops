# Redis configuration
REDIS_HOST=redis
REDIS_PORT=6379

# API configuration (frontend uses this to find the API)
API_URL=http://api:8000

# Port to expose the frontend on your machine
FRONTEND_PORT=3000